---
:backends:
  - yaml
:hierarchy:
  - nodes/"%{::clientcert}"
  - "%{::environment}"
  - users
  - groups
  - usergroups
  - ssh_keys
  - defaults

:yaml:
  :datadir: '/etc/puppet/environments/%{::environment}/hieradata'
